<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/back"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication_UI"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity_home"
            android:exported="false" />
        <activity
            android:name=".Setting"
            android:exported="false" />
        <activity
            android:name=".Menu.setting.MainActivity3_setting"
            android:exported="false" />
        <activity
            android:name=".MainActivity_seed_watch"
            android:exported="false" />
        <activity
            android:name=".Menu.Machine_watch.MainActivity_Machine_watch"
            android:exported="false" />
        <activity
            android:name=".MainActivity_bozhongzuoye"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="false" /> <!-- 完成组件在清单中的注册 -->
        <!-- 启动默认的Activity -->
        <activity
            android:name=".Menu.MainActivity_Menu"
            android:exported="true">

            <!-- 激活默认的Activity -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".MainActivity2"
            android:exported="false" />
        <activity android:name=".DataInputActivity"
            android:exported="false" />
    </application>

</manifest>